#pragma once

// Skeleton B+Tree interface — replace with full implementation later.
class BPlusTree {
public:
    void insert(int key, int record_id) {/* TODO */}
    int find(int key) const { /* TODO */ return -1; }
    // range query etc.
}; 
